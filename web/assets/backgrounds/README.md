# Background Assets

Place the custom background image at:

```text
web/assets/backgrounds/user-background.png
```

The website applies this image as a low-contrast scene layer behind the puzzle UI. Keep it decorative only: it should not contain puzzle answers, category hints, course claims, or readable project evidence.
